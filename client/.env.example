# ================================
# CareerX Client Environment
# ================================
# Copy this file to .env.local and update if needed:
#   cp .env.example .env.local

# Backend API URL (must match your server's PORT)
NEXT_PUBLIC_API_URL=http://localhost:5000/api
